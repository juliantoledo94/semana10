import React from 'react';
import styled from 'styled-components';

const StyledButton = styled.button`
    background-color: ${props => props.primary ? "#2498db" : "#ccc"} ;
    
`;

const Boton = () => {
  return (
    {/* <StyledButton primary>button</StyledButton> */}
  );
}

export default Boton;
